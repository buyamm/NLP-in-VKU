# endpoints.yml
# Khai báo các endpoint kết nối bên ngoài.
# Bỏ comment dòng nào khi cần dùng.

# Action server (dùng khi có custom action Python)
# action_endpoint:
#   url: "http://localhost:5055/webhook"

# Tracker store (mặc định dùng in-memory, không cần khai báo)
# tracker_store:
#   type: redis
#   url: localhost
#   port: 6379

# Event broker
# event_broker:
#   type: pika
#   url: localhost
